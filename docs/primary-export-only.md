[ESLint plugin](index.md) / primary-export-only

# primary-export-only

Ensures that export name matching filename is the only export.

## eslintrc.js

```ts
"@skylib/primary-export-only": "error"
```
